你是一个专业的全栈工程师，请基于我现有的项目架构（后端 Flask + PostgreSQL + Neo4j + Mem0 + Agno + family_mcp + SocketIO）为我生成完整前端代码工程。

本次任务包含两个前端：

A. Web 前端（Next.js + React + Tailwind）
B. 微信小程序（原生小程序 + CloudBase）

并新增以下重要功能：

==================================================
【1. 家庭用户界面（Web 端）】
==================================================
- 登录 / 扫码登录（微信扫码 -> Web Session）
- 首页：展示“家庭记忆仓总览 + 今日 AI 建议 + 图谱入口”
- 家庭图谱（Three.js 可视化 Neo4j）
- 家庭记忆时间轴（支持搜索、过滤、按成员筛选）
- AI 聊天窗口（SocketIO 实时回复）
- 家人资料卡片展示（来自 /api/v1/family 等接口）
- 家庭记忆管理功能：
  - 手动新增记忆
  - 修改/删除
  - 事件分类：生活、情感、健康、节日、工作等
- AI 分身配置（Persona 设置）
- AI 情绪播报（展示 AI 推理的亲密度/情绪标签）
- 通知中心（Socket 推送的提示）

==================================================
【2. 管理员后台（Web 端 Admin Dashboard）】
==================================================
技术栈：Next.js + Tailwind + shadcn/ui + echarts

要求包含以下模块：

▶ 系统总览仪表盘（Dashboard）
- 后端健康检查状态（/ping）
- Redis / PostgreSQL / Neo4j 连接状态
- LLM / Embedding 服务耗时
- Mem0 记忆同步状态
- MCP 插件可用性状态
- SocketIO 在线家庭数量
- 当前服务 CPU / 内存（使用后端提供的 metrics）

▶ 八大核心目标功能监控
需为每个目标建立状态卡片 + 最近日志流：

1. 数据库与模型搭建（pg、pgvector、neo4j）
2. 后端微服务（family_api, graph_api, memory_api, mcp_api）
3. AI 能力层（Mem0、Agno）
4. 前端图谱与 UI 层
5. 硬件适配与 WebSocket
6. MCP 插件封装
7. 系统联调与测试
8. 腾讯云部署

每一项需包含：
- 当前状态（ok / warning / fail）
- 最近一次执行时间
- 最近执行日志（从后台 /logs 接口获取）
- 最近 24 小时请求量折线图
- 错误率统计

▶ 系统日志中心（Log Center）
- 汇总全部后端日志
- 分模块过滤
- 按关键字搜索
- 分页展示
- 支持下载

▶ 流量监控（Traffic / API Analytics）
- 每分钟请求量
- API 响应时间分布
- 错误堆叠图
- 资源占用（后端提供 metrics）
- 每个家庭的活跃度排行

▶ 家庭 & 用户管理
- 用户信息（微信 openid / 注册来源 / 最近活跃）
- 家庭信息管理（家庭结构图、使用情况）
- 分身（Persona）管理
- 数据导出

==================================================
【3. 微信小程序前端】
==================================================
小程序需实现：

- 微信登录（openid -> 我们的 CloudBase）
- 我的家庭（成员列表）
- 家庭记忆列表
- AI 对话（走 WebSocket 或 REST 模式）
- 家庭时间轴（简化版）
- 家人相册（未来扩展）
- 老人模式（大字体）

并要求与 Web 一致采用：
- 用户体系（CloudBase 身份验证）
- 家庭数据读写 API

==================================================
【4. 前端工程结构要求】
==================================================
Web（Next.js）结构：

/app
   /dashboard         → 管理员仪表盘
   /chat              → AI 聊天窗口
   /graph             → 家庭图谱 (Three.js)
   /timeline          → 家庭记忆时间轴
   /family            → 家庭成员资料
   /persona           → AI 分身设置
   /admin/logs        → 日志中心
   /admin/monitor     → 系统监控
   /auth/login
   /auth/callback

components/
hooks/
lib/
utils/
services/           → axios 客户端，封装后端 API

==================================================
【5. WebSocket 规范】
==================================================
监听事件：
- chat_reply
- family_graph
- notice_message

发送事件：
- chat_message
- get_graph_update

==================================================
【6. 需要你输出的内容】
==================================================

请生成以下内容：

(1) Next.js 项目完整结构
(2) 每个页面的代码（jsx/tsx）
(3) 全局 SocketIO 客户端代码
(4) 全局 API 客户端 service 文件
(5) Three.js 家庭图谱组件
(6) 管理员仪表盘的完整前端页面代码
(7) 日志中心（Log Center）组件
(8) Echarts 折线图 / 饼图组件
(9) 小程序完整文件结构 + 代码
(10) 所有核心页面的 UI 设计（Tailwind）
(11) 全套接口调用示例
(12) 打包与部署脚本（Vercel + 腾讯云）

==================================================
【注意】
1. 输出必须按照真实可运行工程设计
2. 不要省略代码
3. 管理员后台必须结构完整、功能齐全
4. 所有页面使用简洁现代 UI 设计
5. 若某处需要后端支持，请自动补充接口
==================================================

开始生成完整前端工程。
